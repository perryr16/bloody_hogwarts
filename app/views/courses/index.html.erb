<h1>COURSES INDEX</h1>

<% @courses.each do |course| %>
  <h3>Course Name: <%= course.name %></h3>
  <h2><%= course.name  %> student count: <%= course.students.count %></h2>
  <% course.students.order(:name).each do |student| %>
  <p><%= student.name %></p>
  <% end %>
<% end %>
